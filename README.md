# Конспект


---

## **1. Стек TCP/IP** 📦
*Многоуровневая модель передачи данных*

| **Уровень**              | **Протоколы**                     | **Функции**                                                                 |
|--------------------------|-----------------------------------|-----------------------------------------------------------------------------|
| **Прикладной**           | HTTP, HTTPS, FTP, DNS, SSH        | Взаимодействие приложений (браузер ↔ сервер)                                |
| **Транспортный**         | TCP, UDP                          | Гарантированная/быстрая доставка, управление потоками                       |
| **Сетевой (Интернет)**   | IP, ICMP, IGMP                    | Маршрутизация, логическая адресация (IPv4/IPv6)                             |
| **Канальный**            | Ethernet, Wi-Fi, MAC, PPP, ARP    | Физическая передача данных в локальной сети                                 |

### **История и назначение**
* 🏛️ **Происхождение**: Разработан Министерством обороны США в 1970-х годах
* 🎯 **Назначение**: Обеспечение надежной передачи данных в разнородных сетях

### **Детальное описание уровней**

#### **Прикладной уровень**
* 🔄 **Функции**: Предоставляет приложениям доступ к сетевым службам
* 📝 **Протоколы**:
  * HTTP/HTTPS: Передача веб-страниц
  * FTP: Передача файлов
  * SMTP/POP3/IMAP: Электронная почта
  * DNS: Преобразование доменных имен в IP-адреса
  * SSH: Безопасное удаленное управление

#### **Транспортный уровень**
* 🚚 **Функции**: Управление передачей данных между конечными системами
* 📝 **Протоколы**:
  * TCP: Надежная доставка, заголовок 20-60 байт
  * UDP: Быстрая доставка, заголовок всего 8 байт

#### **Сетевой уровень**
* 🧭 **Функции**: Определение пути передачи данных
* 📝 **Протоколы**:
  * IPv4: 32-битные адреса (4,3 млрд уникальных адресов)
  * IPv6: 128-битные адреса (340 ундециллионов адресов)
  * ICMP: Отправка сообщений об ошибках и проверка соединения (ping)

#### **Канальный уровень**
* 🔌 **Функции**: Управление доступом к физической среде
* 📝 **Протоколы**:
  * Ethernet: Наиболее распространенный протокол LAN
  * MAC: Адресация на физическом уровне (48-битный адрес)
  * ARP: Преобразование IP-адресов в MAC-адреса

### **Процесс инкапсуляции данных** 📩
1. **Прикладной уровень**: Данные
2. **Транспортный уровень**: Данные → Сегмент (+ TCP/UDP-заголовок)
3. **Сетевой уровень**: Сегмент → Пакет (+ IP-заголовок)
4. **Канальный уровень**: Пакет → Кадр (+ Ethernet-заголовок)

---

## **2. Транспортный уровень** 🔌
*TCP vs UDP, обеспечение доставки данных*

### **TCP (Transmission Control Protocol) - Надёжный**
* ✅ **Особенности**:
  * 3-way handshake (`SYN` → `SYN-ACK` → `ACK`)
  * Подтверждение доставки (ACK)
  * Управление перегрузками (окно перегрузки)
  * Нумерация последовательностей (sequence numbers)
  * Таймеры повторной передачи (retransmission timers)
* 📍 **Использование**: Веб-страницы, почта, файловые передачи
* 🔄 **Закрытие соединения (4-way handshake)**:
  1. `FIN` (инициатор закрытия)
  2. `ACK` (ответ на FIN)
  3. `FIN` (ответное закрытие)
  4. `ACK` (подтверждение)

### **UDP (User Datagram Protocol) - Быстрый**
* ⚡ **Особенности**:
  * Нет установления соединения
  * Минимальные задержки (заголовок 8 байт)
  * Отсутствие перегрузки сети из-за повторных отправок
* 📍 **Использование**: Стриминг, VoIP, онлайн-игры, DNS
* 📊 **Структура заголовка**:
  * Порт источника (16 бит)
  * Порт назначения (16 бит)
  * Длина (16 бит)
  * Контрольная сумма (16 бит)

---

## **3. HTTP vs HTTPS** 🔐
*Безопасность в вебе*

| **Характеристика**       | **HTTP**                          | **HTTPS**                         |
|--------------------------|-----------------------------------|-----------------------------------|
| **Шифрование**           | ❌ Нет                            | ✅ TLS/SSL                         |
| **Порт**                 | 80                                | 443                               |
| **Сертификаты**          | Не требуется                      | Обязательны (Let's Encrypt и др.) |
| **Производительность**   | Быстрее                          | Медленнее (рукопожатие TLS)       |

### **HTTP (HyperText Transfer Protocol)**
* 📚 **Версии**:
  * HTTP/1.0: Одно соединение - один запрос
  * HTTP/1.1: Постоянные соединения (keep-alive)
  * HTTP/2: Мультиплексирование, сжатие заголовков
  * HTTP/3: Использует QUIC вместо TCP (UDP-основа)

### **HTTPS (HTTP Secure)**
* 🔒 **Механизм работы TLS**:
  1. **Рукопожатие (Handshake)**:
     * `ClientHello` → `ServerHello`
     * Проверка сертификата
     * Выбор шифронабора (cipher suite)
     * Генерация сеансовых ключей
  2. **Шифрование**:
     * Симметричное шифрование данных (AES)
     * Асимметричное для обмена ключами (RSA, ECDHE)
  3. **Целостность данных**: HMAC (Hash-based Message Authentication Code)

---

## **4. Структура HTTP** 📄
*Пример запроса и ответа*

### **Запрос HTTP**
```http
GET /api/data HTTP/1.1
Host: example.com
User-Agent: Mozilla/5.0
Accept: application/json
```

### **Ответ HTTP**
```http
HTTP/1.1 200 OK
Date: Mon, 27 Jul 2023 12:28:53 GMT
Server: Apache/2.2.14 (Win32)
Content-Type: application/json
Content-Length: 88

{
  "status": "success",
  "data": {
    "message": "Hello World"
  }
}
```

### **Общая структура HTTP-сообщений**
* **Запрос**:
```
[МЕТОД] [ПУТЬ] HTTP/[ВЕРСИЯ]
[Заголовок1]: [Значение1]
[Заголовок2]: [Значение2]
...
[Пустая строка]
[Тело запроса]
```

* **Ответ**:
```
HTTP/[ВЕРСИЯ] [КОД СОСТОЯНИЯ] [ТЕКСТ СОСТОЯНИЯ]
[Заголовок1]: [Значение1]
[Заголовок2]: [Значение2]
...
[Пустая строка]
[Тело ответа]
```

### **Элементы заголовка HTTP** 📋

#### **Общие заголовки**
* `Date`: Дата и время сообщения
* `Connection`: Управление соединением (keep-alive, close)
* `Cache-Control`: Директивы кэширования
* `Transfer-Encoding`: Метод передачи (chunked)

#### **Заголовки запроса**
* `Host`: Имя хоста и порт сервера
* `User-Agent`: Идентификация клиента
* `Accept`: Предпочитаемые типы данных
* `Authorization`: Данные аутентификации
* `Cookie`: Данные состояния клиента
* `Referer`: URL источника запроса

#### **Заголовки ответа**
* `Server`: Информация о сервере
* `Set-Cookie`: Установка cookie
* `Location`: URL для перенаправления
* `Content-Type`: MIME-тип содержимого
* `Content-Length`: Размер тела в байтах
* `Expires`: Срок действия содержимого

### **CORS (Cross-Origin Resource Sharing)** 🔀
* 🔍 **Определение**: Механизм для разрешения кросс-доменных запросов
* ⚠️ **Проблема**: Правило ограничения домена (Same-Origin Policy) в браузерах
* 🛠️ **Основные заголовки**:
  * `Access-Control-Allow-Origin`: Разрешенные домены
  * `Access-Control-Allow-Methods`: Разрешенные методы
  * `Access-Control-Allow-Headers`: Разрешенные заголовки
  * `Access-Control-Allow-Credentials`: Передача учетных данных
* 🔄 **Процесс**:
  1. **Простой запрос**: Сразу отправляется к серверу
  2. **Предварительный запрос** (preflight): Метод OPTIONS для проверки разрешений

---

## **5. API (Application Programming Interface)** 🧩
*Интерфейс взаимодействия между системами*

### **Уровень работы API**
* 🔝 **API работает на прикладном уровне** стека TCP/IP
* 🔄 **Взаимодействие**: Связывает приложения через стандартизированные протоколы

### **Сущность API**
* 📚 **Определение**: Набор определений, протоколов и инструментов для создания приложений
* 🛠️ **Функция**: Абстрагирование сложных систем и предоставление простых интерфейсов
* 🧰 **Компоненты**:
  * **Endpoints**: URL-адреса для доступа к ресурсам
  * **Методы**: GET, POST, PUT, DELETE и т.д.
  * **Данные**: Структуры запросов и ответов
  * **Аутентификация**: OAuth, API-ключи, JWT
  * **Документация**: Описание использования API

### **Типы API**
* 🔓 **По доступу**:
  * **Открытые**: Доступны всем (Twitter API, Google Maps API)
  * **Партнерские**: Доступны партнерам компании
  * **Внутренние**: Доступны только внутри организации

* 🏗️ **По архитектуре**:
  * **REST API**: Использует HTTP-методы (самый популярный)
  * **SOAP API**: XML-формат, строгая типизация
  * **GraphQL**: Запрос только необходимых данных
  * **gRPC**: Бинарный протокол на основе HTTP/2 (быстрый)
  * **WebSocket API**: Для двустороннего обмена в реальном времени
  * **Webhook**: Обратные вызовы через HTTP

### **Версионирование API** 🔢
* 🎯 **Цель**: Обеспечение обратной совместимости
* 🛠️ **Методы**:
  * **URL-версионирование**: `/api/v1/resource`
  * **Заголовок**: `Accept: application/vnd.company.v1+json`
  * **Параметр запроса**: `?version=1`

---

## **6. REST и SOAP** 📡
*Архитектурные подходы к созданию API*

### **REST (Representational State Transfer)** 🔄
* 📚 **Определение**: Архитектурный стиль для распределенных гипермедиа-систем
* 👨‍💼 **Автор**: Рой Филдинг, 2000 год (диссертация)
* 🧱 **Основные принципы (ограничения)**:
  1. **Клиент-серверная архитектура**: Разделение ответственности
  2. **Отсутствие состояния** (Statelessness): Каждый запрос содержит всю необходимую информацию
  3. **Кэширование**: Явное указание кэшируемости ответов
  4. **Единообразие интерфейса**:
     * Идентификация ресурсов (URI)
     * Манипуляции ресурсами через представления
     * Самоописываемые сообщения
     * HATEOAS (Hypermedia as the Engine of Application State)
  5. **Слоистая система**: Клиент не знает, взаимодействует ли он напрямую с сервером

### **SOAP (Simple Object Access Protocol)** 📨
* 📚 **Определение**: Протокол обмена структурированными сообщениями
* 🏢 **Стандарт**: W3C
* 📄 **Структура сообщения**:
  * **Envelope** (Конверт): Корневой элемент XML-сообщения
  * **Header** (Заголовок): Необязательная информация (аутентификация)
  * **Body** (Тело): Содержит вызываемые методы и параметры
  * **Fault** (Ошибка): Информация об ошибках (необязательно)

### **Сравнение REST и SOAP** ⚖️

| **Аспект**              | **REST**                           | **SOAP**                          |
|--------------------------|-----------------------------------|-----------------------------------|
| **Философия**            | Архитектурный стиль               | Строгий протокол                  |
| **Формат данных**        | Обычно JSON (легкий)              | Исключительно XML (объемный)      |
| **Транспорт**            | Обычно HTTP/HTTPS                 | HTTP, SMTP, TCP, UDP и др.        |
| **Контракт сервиса**     | Слабый контракт (документация)    | Строгий контракт через WSDL       |
| **Безопасность**         | Через HTTP (TLS/SSL)              | WS-Security (встроенный стандарт) |
| **Транзакции**           | Не поддерживает встроенные        | Поддерживает (WS-AtomicTransaction)|
| **Производительность**   | Легкий и быстрый (особенно JSON)  | Больше накладных расходов         |
| **Использование**        | Мобильные, веб, облако, соцсети   | Корпоративные, финансовые системы |
| **Кэширование**          | Встроенная поддержка через HTTP   | Не поддерживает по умолчанию      |
| **Легкость изучения**    | Относительно прост                | Более сложный                     |
